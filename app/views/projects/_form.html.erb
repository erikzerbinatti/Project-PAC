<%= form_for(@project) do |f| %>
 
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :responsible_id %><br>
    <%= f.collection_select(:responsible_id, @responsibles, :id, :name) %>
  </div>
  <div>
    <%= f.submit class:"btn btn-add"%>
    <%= link_to '<< Voltar a lista de projetos', projects_path, class:"btn btn-voltar"%>
  </div>
<% end %>